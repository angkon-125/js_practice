// datatype 

// number 

let num = 10;
let num2 = 20;

console.log(num + num2); // 

// string 

const firstName = "Armaan";
const lastName = "Jamaan";

console.log(firstName + ' ' + lastName);

// boolean 

let login = true;
let logout = false;

console.log(login);
console.log(logout);

// in javascript boolean is a data type that can only have two values: true or false
// true = 1
//false = 0
// if i malepulate the boolean value it will be converted to 1 or 0
// for example 
let x = 10;
let y = 20;
if (x > y) {
    console.log(true);
}

// undefined 

// null
 let a = null;

 console.log(x);

 undefined = null;

 // null and undefined are different
// null is an object
// undefined is a type
// null is a value

// object 
let person = {
    firstName:  'Armaan',
    lastName: 'Jamaan',
    age : 25,
    login: true,
    logout: false,
}
console.log(person.firstName);
console.log(person.lastName);
console.log(person.age);
console.log(person.login);
console.log(person.logout);

// this is the way to call values from the object